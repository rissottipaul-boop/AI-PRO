name: Agent Task
description: Структурированная задача для Copilot агента
labels: ["agent-task"]
body:
  - type: markdown
    attributes:
      value: |
        ## 🤖 Задача для автономного агента
        
        Этот шаблон используется для формализации задач, которые может выполнять Copilot coding agent.
        Заполните все обязательные поля для обеспечения предсказуемого результата.

  - type: textarea
    id: context
    attributes:
      label: Контекст / Обоснование
      description: |
        Почему это нужно? Какую проблему решаем?
        Ссылки на связанные issues, ADR, документацию.
      placeholder: |
        Проблема: текущая реализация X не поддерживает Y
        Цель: добавить поддержку Y для улучшения Z
        Связано с: #123, ADR-0001
    validations:
      required: true

  - type: textarea
    id: scope
    attributes:
      label: Область изменений (Scope)
      description: |
        Что ВКЛЮЧЕНО и что ЯВНО ИСКЛЮЧЕНО из задачи.
        Это помогает агенту понять границы работы.
      placeholder: |
        ✅ Включено:
        - Добавить функцию calculate_metrics()
        - Написать unit тесты для новой функции
        - Обновить документацию в README
        
        ❌ Исключено:
        - Рефакторинг существующего кода
        - Изменения в публичном API
        - Миграция на новую версию зависимостей
    validations:
      required: true

  - type: textarea
    id: acceptance
    attributes:
      label: Критерии приёмки (Acceptance Criteria)
      description: |
        Измеримый набор условий для завершения задачи.
        Используйте чеклист для ясности.
      placeholder: |
        - [ ] Функция calculate_metrics() реализована и типизирована
        - [ ] Покрытие тестами >= 85%
        - [ ] Все линтеры проходят (ruff, black, mypy)
        - [ ] Документация обновлена
        - [ ] CI проходит успешно
      value: |
        - [ ] 
    validations:
      required: true

  - type: dropdown
    id: priority
    attributes:
      label: Приоритет
      description: Насколько срочна задача?
      options:
        - low
        - medium
        - high
        - critical
      default: 1
    validations:
      required: true

  - type: dropdown
    id: task_type
    attributes:
      label: Тип задачи
      description: Категория работы
      options:
        - feature (новая функциональность)
        - fix (исправление бага)
        - refactor (рефакторинг)
        - test (улучшение тестов)
        - docs (документация)
        - chore (инфраструктура)
        - perf (производительность)
        - security (безопасность)
      default: 0
    validations:
      required: true

  - type: textarea
    id: risks
    attributes:
      label: Риски и граничные случаи
      description: |
        Возможные проблемы и как их минимизировать.
        Граничные случаи, которые нужно учесть.
      placeholder: |
        Риски:
        - Изменение может сломать обратную совместимость
        - Новая зависимость может конфликтовать с существующими
        
        Митигация:
        - Добавить тесты на backward compatibility
        - Зафиксировать версии зависимостей
        
        Граничные случаи:
        - Пустой input
        - None values
        - Очень большие значения (>1M элементов)
    validations:
      required: false

  - type: textarea
    id: dependencies
    attributes:
      label: Зависимости
      description: |
        От каких других задач или изменений зависит эта задача?
        Блокирующие факторы.
      placeholder: |
        Зависит от:
        - #123 должен быть закрыт первым
        - ADR-0003 должен быть утверждён
        
        Блокирует:
        - #456
    validations:
      required: false

  - type: input
    id: estimated_time
    attributes:
      label: Оценка времени
      description: Примерное время выполнения (опционально)
      placeholder: "2h, 1d, 3d"
    validations:
      required: false

  - type: textarea
    id: notes
    attributes:
      label: Дополнительные заметки
      description: |
        Любая дополнительная информация:
        - Ссылки на примеры кода
        - Ссылки на внешние ресурсы
        - Особые инструкции для агента
      placeholder: |
        Полезные ссылки:
        - Похожая реализация: https://github.com/example/repo
        - Документация API: https://docs.example.com
        
        Особые требования:
        - Использовать asyncio для операций I/O
        - Следовать паттерну из existing_module.py
    validations:
      required: false

  - type: checkboxes
    id: agent_checklist
    attributes:
      label: Pre-flight checklist
      description: Убедитесь, что задача готова для агента
      options:
        - label: Задача чётко сформулирована и атомарна
          required: true
        - label: Критерии приёмки измеримы
          required: true
        - label: Область изменений определена (scope)
          required: true
        - label: Прочитаны `.github/COPILOT_INSTRUCTIONS.md` и `automation_policy.yaml`
          required: false
        - label: Связанная документация обновлена (если нужно)
          required: false
